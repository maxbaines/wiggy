# Loop Configuration
# Copy this file to .env and fill in your values

# Required: Anthropic API Key
ANTHROPIC_API_KEY=your-api-key-here

# Optional: Model to use (default: claude-sonnet-4-20250514)
RALPH_MODEL=claude-sonnet-4-5

# Optional: Max tokens per response (default: 8192)
RALPH_MAX_TOKENS=8192

# Optional: Working directory (default: current directory)
RALPH_WORKING_DIR=.

# Optional: PRD file path (default: auto-detect)
# RALPH_PRD_FILE=plans/prd.json

# Optional: Progress file path (default: progress.txt)
RALPH_PROGRESS_FILE=progress.txt

# Optional: Enable verbose logging (default: false)
RALPH_VERBOSE=false
